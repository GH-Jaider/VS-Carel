# Files excluded from the VSIX. Everything else in this package ships.

# Sources and build config: the extension ships bundled as dist/extension.js
src/
tsconfig.json
webpack.config.js
out/

# Bundled by webpack, never needed at runtime
node_modules/

# Source maps
**/*.map
